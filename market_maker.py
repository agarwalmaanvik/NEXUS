# market_maker.py — DELETED from decision path in NEXUS v2.
# The bid/ask spread model (PokerHeart) has no valid poker meaning.
# Bet sizing is now handled by bet_sizer.py (arithmetic pot fractions).
# This file is kept as a stub to avoid ImportError from any legacy callers.

class PokerHeart:
    def get_market_report(self, *args, **kwargs):
        return {}  # Legacy stub — no longer used
